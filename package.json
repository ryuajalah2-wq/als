{
  "name": "crypto-market-analyzer",
  "version": "1.0.0",
  "description": "Full-stack crypto market analysis application with technical indicators",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=backend && npm install --workspace=frontend",
    "start": "npm run start --workspace=backend & npm run start --workspace=frontend",
    "backend": "npm run start --workspace=backend",
    "frontend": "npm run start --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  }
}
